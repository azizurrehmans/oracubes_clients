ALTER TABLE GARMOR.MONTHLY_ATTENDANCE
RENAME COLUMN EMPLOYEE_NAME TO NAME;

ALTER TABLE GARMOR.MONTHLY_ATTENDANCE
 ADD (designation  VARCHAR2(100));

ALTER TABLE GARMOR.MONTHLY_ATTENDANCE
 ADD (department  VARCHAR2(100));

ALTER TABLE GARMOR.MONTHLY_ATTENDANCE
 ADD (month_days  NUMBER(2));

ALTER TABLE GARMOR.MONTHLY_ATTENDANCE
 ADD (sl  NUMBER(5,2));

ALTER TABLE GARMOR.MONTHLY_ATTENDANCE
 ADD (remarks  varchar2(100));

ALTER TABLE GARMOR.MONTHLY_ATTENDANCE
 ADD (OTHER_EARNING  number(10));

ALTER TABLE GARMOR.MONTHLY_ATTENDANCE
 ADD (cl  NUMBER(5,2));

